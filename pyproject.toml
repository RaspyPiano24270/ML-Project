[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "stormcast-ml-project"
version = "0.1.0"
description = "StormCast ML forecasting project"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["src"]
